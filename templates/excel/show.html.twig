{% extends 'base.html.twig' %}

{% block title %}Excel{% endblock %}

{% block body %}
    <h1>Excel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ excel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ excel.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('excel_index') }}">back to list</a>

    <a href="{{ path('excel_edit', {'id': excel.id}) }}">edit</a>

    {{ include('excel/_delete_form.html.twig') }}
{% endblock %}
